<Window x:Class="KursovoiTuv.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Портал заказов" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="StatusStyle" TargetType="TextBlock">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="New">
                    <Setter Property="Foreground" Value="Blue"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="InProgress">
                    <Setter Property="Foreground" Value="Orange"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Completed">
                    <Setter Property="Foreground" Value="Green"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Портал заказов производства" 
                       FontSize="16" FontWeight="Bold"/>
            <TextBlock Text=" - МеталлПром Инжиниринг" 
                       FontSize="16" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Основной контент -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Список заказов -->
            <GroupBox Grid.Column="0" Header="Заказы в работе" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                        <Button Content="Добавить заказ" Command="{Binding AddOrderCommand}" 
                                Margin="0,0,10,0" Padding="10,5"/>
                        <Button Content="Рассчитать стоимость" Command="{Binding CalculateCostCommand}"
                                Margin="0,0,10,0" Padding="10,5"/>
                    </StackPanel>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" 
                              SelectedItem="{Binding SelectedOrder}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              Margin="5">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="№ заказа" Binding="{Binding OrderNumber}" Width="100"/>
                            <DataGridTextColumn Header="Клиент" Binding="{Binding ClientName}" Width="150"/>
                            <DataGridTextColumn Header="Изделие" Binding="{Binding ProductDescription}" Width="200"/>
                            <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="60"/>
                            <DataGridTextColumn Header="Срок" Binding="{Binding Deadline, StringFormat=d}" Width="80"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="New">
                                                <Setter Property="Background" Value="LightBlue"/>
                                                <Setter Property="Padding" Value="5,2"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="InProgress">
                                                <Setter Property="Background" Value="LightYellow"/>
                                                <Setter Property="Padding" Value="5,2"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                <Setter Property="Background" Value="LightGreen"/>
                                                <Setter Property="Padding" Value="5,2"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Стоимость" 
                                              Binding="{Binding Cost, StringFormat='C2'}" 
                                              Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>

            <!-- Детали заказа -->
            <GroupBox Grid.Column="1" Header="Детали заказа">
                <ScrollViewer>
                    <StackPanel Margin="5" DataContext="{Binding SelectedOrder}">
                        <TextBlock Text="Информация о заказе:" FontWeight="Bold" Margin="0,0,0,10"/>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="{Binding OrderNumber}" FontWeight="Bold" FontSize="14"/>
                            <TextBlock Grid.Row="1" Text="{Binding ClientName}" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="2" Text="{Binding ProductDescription}" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="3" Text="{Binding Quantity, StringFormat='Количество: {0} шт.'}" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="4" Text="{Binding Deadline, StringFormat='Срок до: {0:d}'}" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="5" Text="{Binding Cost, StringFormat='Стоимость: {0:C2}'}" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="6" Text="{Binding Status, StringFormat='Статус: {0}'}" Margin="0,5,0,0"/>

                            <Button Grid.Row="7" Content="Следующий статус" 
                                    Command="{Binding DataContext.UpdateStatusCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    Margin="0,20,0,0" Padding="10,5"/>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </GroupBox>
        </Grid>

        <!-- Статус бар -->
        <StatusBar Grid.Row="2" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding Orders.Count, StringFormat='Всего заказов: {0}'}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="Инжиниринг © 2024"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>