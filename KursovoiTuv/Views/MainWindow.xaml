<Window x:Class="KursovoiTuv.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:System="clr-namespace:System;assembly=netstandard"
        Title="Портал заказов" 
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Справка">
                <MenuItem Header="О программе" Command="{Binding OpenAboutCommand}"/>
            </MenuItem>
        </Menu>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,10">
            <TextBlock Text="Портал заказов производства" 
                       FontSize="16" FontWeight="Bold"/>
            <TextBlock Text=" - МеталлФэмили Инжиниринг" 
                       FontSize="16" Margin="10,0,0,0"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <!-- ПАНЕЛЬ ДОБАВЛЕНИЯ ЗАКАЗА -->
                <GroupBox Header="Новый заказ" Margin="0,0,0,10">
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" Text="{Binding NewOrderClient}" 
                                 Padding="5" Margin="5" ToolTip="Клиент"/>
                        <TextBox Grid.Column="1" Text="{Binding NewOrderProduct}" 
                                 Padding="5" Margin="5" ToolTip="Изделие"/>
                        <TextBox Grid.Column="2" Text="{Binding NewOrderQuantity}" 
                                 Padding="5" Margin="5" ToolTip="Количество"/>
                        <ComboBox Grid.Column="3" SelectedValue="{Binding NewOrderPriority}"
                                 Margin="5" ToolTip="Приоритет">
                            <System:String>Высокий</System:String>
                            <System:String>Средний</System:String>
                            <System:String>Низкий</System:String>
                        </ComboBox>
                        <Button Grid.Column="4" Content="➕ Добавить" 
                                Command="{Binding AddOrderCommand}"
                                Padding="10,5" Margin="5"/>
                    </Grid>
                </GroupBox>

                <!-- СПИСОК ЗАКАЗОВ -->
                <GroupBox Header="Заказы в работе">
                    <DataGrid ItemsSource="{Binding Orders}" 
                              SelectedItem="{Binding SelectedOrder}"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              Margin="5" Height="300">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="№ заказа" Binding="{Binding OrderNumber}" Width="100"/>
                            <DataGridTextColumn Header="Клиент" Binding="{Binding ClientName}" Width="150"/>
                            <DataGridTextColumn Header="Изделие" Binding="{Binding ProductDescription}" Width="200"/>
                            <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="60"/>
                            <DataGridTextColumn Header="Срок" Binding="{Binding Deadline, StringFormat=d}" Width="80"/>
                            <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </StackPanel>

            <!-- ДЕТАЛИ ЗАКАЗА -->
            <GroupBox Grid.Column="1" Header="Детали заказа">
                <ScrollViewer>
                    <StackPanel Margin="5" DataContext="{Binding SelectedOrder}">
                        <TextBlock Text="Информация о заказе:" FontWeight="Bold" Margin="0,0,0,10"/>

                        <TextBlock Text="{Binding OrderNumber}" FontWeight="Bold" FontSize="14"/>
                        <TextBlock Text="{Binding ClientName}" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding ProductDescription}" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding Quantity, StringFormat='Количество: {0} шт.'}" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding Deadline, StringFormat='Срок до: {0:d}'}" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding Cost, StringFormat='Стоимость: {0:C2}'}" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding Status, StringFormat='Статус: {0}'}" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding Priority, StringFormat='Приоритет: {0}'}" Margin="0,5,0,0"/>

                        <!-- КНОПКИ УПРАВЛЕНИЯ СТАТУСОМ -->
                        <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                            <Button Content="⬅️ Предыдущий" 
                        Command="{Binding DataContext.PreviousStatusCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        Margin="0,0,5,0" Padding="10,5"
                        Visibility="{Binding CanMovePrevious, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                            <Button Content="Следующий ➡️" 
                        Command="{Binding DataContext.NextStatusCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                        Margin="5,0,0,0" Padding="10,5"
                        Visibility="{Binding CanMoveNext, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>

                        <!-- КНОПКА РАСЧЕТА СТОИМОСТИ -->
                        <Button Content="💰 Пересчитать стоимость" 
                    Command="{Binding DataContext.CalculateCostCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    Margin="0,10,0,0" Padding="10,5"
                    Background="#27ae60" Foreground="White"/>
                    </StackPanel>
                </ScrollViewer>
            </GroupBox>
        </Grid>

        <StatusBar Grid.Row="3" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding Orders.Count, StringFormat='Всего заказов: {0}'}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="Инжиниринг © 2025"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>